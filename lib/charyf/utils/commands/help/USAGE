The most common Charyf commands are:
 console      Start the Charyf console (short-cut alias: "c")

 new          Create a new Charyf application. "charyf new my_app" creates a
               new application called MyApp in "./my_app"
 install      Create a new Charyf application. "charyf install" installs charyf
               to a current application in "./charyf"

All commands can be run with -h (or --help) for more information.
In addition to those commands, there are:

